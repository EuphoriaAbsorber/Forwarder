import 'package:flutter_test/flutter_test.dart';

void main() {

  testWidgets('Golden test', (tester) async {
    // await tester.pumpWidget(const AnyWayApp());
    // await expectLater(find.byType(AnyWayApp),
    //     matchesGoldenFile('main.png'));
  });
}